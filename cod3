import java.util.Random;
import java.util.Arrays;


  public class UsoOrdenamiento{
      public static void main(String[] args){
          ArregloOrdenamiento arreglo1= new ArregloOrdenamiento(10);
          arreglo1.llenarAleatorio(20);
          System.out.println("Arreglo original");
          System.out.println("Arreglo1" + arreglo1);
          arreglo1.bubbleS(arreglo.datos);
           System.out.println("Arreglo ordenado" + arreglo1);
      }
  }

public class ArregloOrdenamiento {

    public int[] datos;
    // Constructor 1: recibe la cantidad de elementos
    public Arreglo(int cantidad) {
        datos = new int[cantidad];
    }

    // Constructor 2: recibe un arreglo ya existente
    public Arreglo(int[] datos) 
    {
        this.datos = datos;
    }

    //Ordenamiento por burbuja
    public void bubbleS(int[] datos)  {
        for (int pasadas=1;pasadas<datos.length; pasadas++)   //repite el proceso
        {
           for (int i=0; i<datos.length-1;i++)
           {
              if(datos[i]>datos[i+1])
              {
                int temp = datos[i];
                datos[i]=datos[i+1];
                datos[i+1]=temp;
              }    
           }    
        }
    }

    public void selectionSort(int[] datos) {
    int n = datos.length;

    for (int i = 0; i < n - 1; i++) {
        int indiceMinimo = i;

        for (int j = i + 1; j < n; j++) {
              if (arreglo[j] < arreglo[indiceMinimo]) {
                indiceMinimo = j;
               }  //if
        }//for j
        // swap
        int temp = arreglo[indiceMinimo];
        arreglo[indiceMinimo] = arreglo[i];
        arreglo[i] = temp;
    }   //del i
    } //fin del m̩todo selectionSort

    // M̩todo para llenar con n̼meros aleatorios
    public void llenarAleatorio(int limite) {
        Random random = new Random();
        for (int i = 0; i < datos.length; i++) {
            datos[i] = random.nextInt(limite);
        }
    }

    @Override
    public String toString() {
        return Arrays.toString(datos);
    }
}
